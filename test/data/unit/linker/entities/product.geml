import types

entity product
  with
    autoId
    atLeastOneNotNull([ 'name', 'email' ])
  has
    name 
    email  
    desc -- 'Description'
    attr : nonEmptyText |=defaultGenerator(@latest.email) |>processBeforeValidation |~isSomething |>someProcess('text') 
